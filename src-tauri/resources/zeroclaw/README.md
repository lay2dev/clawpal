Place zeroclaw sidecar binaries in this directory for bundling.

Expected filenames:
- macOS/Linux: `zeroclaw-decider`
- Windows: `zeroclaw-decider.exe`

The install orchestrator will auto-detect these files from bundled resources.
