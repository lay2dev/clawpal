# Prompt Templates

所有 prompt 模板统一放在本目录，按场景分子目录命名：

- `doctor/`：诊断领域
- `install/`：安装领域
- `error-guidance/`：业务失败解释/建议
- `frontend/`：前端组装并发送给后端的会话模板

每个 `.md` 文件必须包含：

1. 文件头两行：
   - `使用位置：...`
   - `使用时机：...`
2. 一个 ` ```prompt ` 代码块作为可执行模板正文

模板变量统一使用 `{{token}}` 占位符，由代码侧加载器替换。
